{
  "devDependencies": {
    "cypress": "^5.6.0",
    "cypress-xpath": "^1.6.1"
  },
  "scripts": {
    "cy:ui": "cypress run --spec 'cypress/integration/UI/**/*' --headless --browser chrome",
    "cy:api": "cypress run --spec 'cypress/integration/API/**/*' --headless --browser chrome",
    "cy:all": "cypress run --spec 'cypress/integration/**/*' --headless --browser chrome",
    "cy:wip": "cypress run --spec 'cypress/integration/UI/article/**/*' --headless --browser chrome"
  }
}
